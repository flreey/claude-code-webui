# Claude Code WebUI Environment Configuration

# Enable dangerous bypass permissions mode (use with extreme caution)
# When enabled, users can select "bypass mode" which allows Claude to execute
# all operations without user approval. This can be dangerous in production.
# Uncomment the following line to enable:
# VITE_ENABLE_BYPASS_MODE=true